import React from "react";

import { ProductsType } from "@/type";

type ProductProps = {
  item: {
    params: ProductsType;
  };
};

export default function page() {
  return <div>{/* Fetch content product */}</div>;
}
